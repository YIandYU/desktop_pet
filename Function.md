# 桌面宠物程序 - 功能文档

> Q 版像素钢铁侠桌面宠物，全屏透明置顶，支持硬件监测、系统托盘、开机自启动。

---

## 文件结构

```
desktop_pet/
├── main.py               # 程序主入口（窗口管理、置顶守护、事件循环）
├── pet.py                # 宠物核心类（状态机、AI行为、交互逻辑）
├── pixel_sprite.py       # 钢铁侠像素精灵（7种状态 + 蓝色阴影）
├── hardware_monitor.py   # 硬件实时监测
├── bubble.py             # 对话气泡
├── config.py             # 配置管理
├── system_tray.py        # 系统托盘（图标、右键菜单）
├── auto_start.py         # 开机自启动管理
├── Requirements.md       # 需求文档
├── Function.md           # 功能文档
└── Dependency.md         # 依赖清单
```

---

## 各文件功能说明

### 1. main.py
- 全屏透明窗口（SRCALPHA + WS_EX_LAYERED）
- 后台线程每 0.1 秒强制置顶
- 隐藏任务栏图标
- 60FPS 主循环处理鼠标/键盘事件
- 隐藏/显示切换
- 退出时自动保存状态

### 2. pet.py
- 7 种状态的状态机（含独立 angry 状态）
- AI 行为：随机走动、日夜切换、无聊入睡
- 交互：单击/双击/长按/拖拽/右键
- 长按触发 STATE_ANGRY，持续 0.5 秒后恢复待机

### 3. pixel_sprite.py
- Q 版像素钢铁侠，7 种动画
- 金红配色 + 蓝色方舟反应炉
- 生气动画：红眼怒视、倒竖眉、咬牙、微晃
- 所有精灵附加 _add_blue_shadow() 蓝色投影阴影
- 开心时眼睛暖色、被拖拽时眼睛发光

### 4. hardware_monitor.py
- 后台线程每 1 秒采集一次
- 数据：CPU/内存/磁盘(全盘符)/GPU/网络/系统/电池/风扇/温度
- 显示顺序：SYS→UPTIME→BATT→CPU→GPU→MEM→DISK→NET→FAN
- GPU 名自动去除 "NVIDIA GeForce" 等前缀
- 采集含 GPU 风扇（nvidia-smi fan.speed）

### 5. bubble.py
- 白底圆角气泡，三角尾巴指向宠物
- 8 类文案（单击/双击/长按/无聊/监测/唤醒/早安/晚安）
- 显示 3 秒自动消失

### 6. config.py
- pet_config.json，保存位置坐标
- 启动自动恢复，超出范围则右下角默认

### 7. system_tray.py
- 宠物待机图 64x64 托盘图标
- 右键菜单：显示宠物✓ / 开机自启动✓ / 作者以及声明 / 退出

### 8. auto_start.py
- 启动文件夹创建快捷方式
- 支持启用/禁用/检查/toggle

---

## 交互操作速查表

| 操作 | 效果 |
|------|------|
| 左键单击 | 开心跳跃 + 掌心炮 + 气泡 |
| 左键双击 | 开心（更久）+ 害羞气泡 |
| 左键长按 | 红眼怒视 + 晃动 + 愤怒气泡 |
| 左键拖拽 | 惊讶表情跟随鼠标 |
| 右键 | 打开/关闭硬件监测面板 |
| ESC | 退出程序 |
| 托盘→显示宠物 | 隐藏/显示窗口 |
| 托盘→开机自启动 | 开机自启动开关 |
| 托盘→作者以及声明 | 弹窗显示作者和免责 |
| 托盘→退出 | 完全退出 |
